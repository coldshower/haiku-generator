function createHaiku(structure_arr, probability_arr) { // format [5, 7, 5]
	function createLine(numSyll) {
		var currSyll = 0;

		while (currSyll < numSyll) {

		}

	}
	var haiku = '';

	structure.forEach(function(syllables) {
		haiku += createLine(syllables);
	});

	return haiku;
}